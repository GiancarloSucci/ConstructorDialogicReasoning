<?xml version="1.0" encoding="UTF-8"?>
<questionPath name="custom-question-dialog">
    <question
        id="first"
        llm_alias="gpt-4o-mini"
        mode="direct"
        role="initial_answer"
        timeout="900"
        request_timeout="30"
        retry_delay="5">
        <description>Produce the first answer.</description>
        <prompt>
Original prompt:
{prompt}

Produce a precise technical answer.
Return only the answer.
        </prompt>
    </question>

    <question
        id="review"
        llm_alias="claude-opus-4-7"
        mode="direct"
        role="review_and_improve"
        timeout="900"
        request_timeout="30"
        retry_delay="5">
        <description>Review and improve the first answer.</description>
        <prompt>
Original prompt:
{prompt}

Previous answer:
{answer}

Prompt used by first:
{first.prompt}

Answer produced by first:
{first.answer}

Check correctness and completeness.
Return a complete improved answer.
        </prompt>
    </question>

    <question
        id="finalizer"
        llm_alias="gpt-4o-mini"
        mode="direct"
        role="final_synthesis"
        timeout="900"
        request_timeout="30"
        retry_delay="5">
        <description>Produce the final answer.</description>
        <prompt>
Original prompt:
{prompt}

Current answer:
{answer}

Review answer:
{review.answer}

Produce the final version.
Return only the final answer.
        </prompt>
    </question>
</questionPath>