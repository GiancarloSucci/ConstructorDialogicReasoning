<?xml version="1.0" encoding="UTF-8"?>
<agentPath name="constructor-stateful-review-chain">
    <endpoint
        id="first"
        llm_alias="gpt-4o-mini"
        mode="direct"
        role="initial_answer"
        timeout="900"
        request_timeout="30"
        retry_delay="5">
        <description>Produce the first answer.</description>
    </endpoint>

    <endpoint
        id="review"
        llm_alias="gemini-2.0-flash"
        mode="direct"
        role="review_and_improve"
        timeout="900"
        request_timeout="30"
        retry_delay="5">
        <description>Check the previous answer and improve correctness and completeness.</description>
    </endpoint>

    <endpoint
        id="finalizer"
        llm_alias="gpt-4o-mini"
        mode="direct"
        role="final_synthesis"
        timeout="900"
        request_timeout="30"
        retry_delay="5">
        <description>Produce the final answer for the user.</description>
    </endpoint>
</agentPath>